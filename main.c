#include "CLI/command_parser.h"
#include "Geometry/triangle.h"

int main(int argc, char *argv[]) {
    parse_commands(argc, argv);
//    char* filename = "simpsonsvr.bmp";
//    char* filename_new = "new.bmp";
//    DIB_Header dibh = parse_dib(filename);
//    Pixels canvas = create_canvas(dibh.image_height, dibh.image_width);
//    canvas_draw(canvas, filename);
//    Pixel p1 = {100, 200, 255, 0, 0};
//    Pixel p2 = {500, 650, 255, 0, 0};
//    Rgb red = {0, 0, 255};
//    draw_thick_line(canvas, p1, p2, red, 10);
//    canvas_write(canvas, filename, filename_new);
    return 0;
}